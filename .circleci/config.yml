version: 2.1

commands:
  ros_build:
    steps:
      - checkout
      - run:
          name: Set Up Container
          command: |
            apt-add-repository -y ppa:astuff/kvaser-linux
            apt-get update -qq && apt-get install -qq dpkg kvaser-canlib-dev
            mkdir $CIRCLE_PROJECT_REPONAME
            mv `find -maxdepth 1 -not -name . -not -name $CIRCLE_PROJECT_REPONAME` $CIRCLE_PROJECT_REPONAME/
      - run:
          name: Install Dependencies
          command: |
            rosdep install --from-paths . --ignore-src -y
      - run:
          name: Build
          command: |
            source `find /opt/ros -maxdepth 2 -name setup.bash | sort | head -1`
            cd ..
            colcon build
      - run:
          name: Run Tests
          command: |
            source `find /opt/ros -maxdepth 2 -name setup.bash | sort | head -1`
            cd ..
            colcon test --packages-select $CIRCLE_PROJECT_REPONAME
            colcon test-result

jobs:
  foxy:
    docker:
      - image: autonomoustuff/docker-builds:foxy-ros-base
    steps:
      - ros_build
    working_directory: ~/src

workflows:
  ros_build:
    jobs:
      - foxy
